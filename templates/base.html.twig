<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
          <link href="http://localhost/library/public/build/app.css" rel="stylesheet"/>
        <script src="http://localhost/library/public/build/app.js" defer></script>
        {% block stylesheets %}<link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet"/>
       {% endblock %}

        {% block javascripts %}{% endblock %}
    </head>
    <body>
    {% if success is defined and success|length > 0 %}
        {% include 'messages/success.html.twig' %}
        {% endif %}
        {% if errors is defined and errors|length > 0 %}
        {% include 'errors/validation.html.twig' %}
        {% endif %}
        {% block body %}{% endblock %}
    </body>
</html>
